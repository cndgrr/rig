# Release drill — 0.4.0 — 2026-09-03

Run ID: drill-2026-09-03-a. Host: Debian GNU/Linux 13 (trixie), 2 vCPU / 4 GB RAM (kvm).
Rig under drill: 0.4.0, built from c85efadc08f7ed2d50462a9b26a06efa23f72a0e — artifact:rig-0.4.0.sh sha256:977302aa735e41ad7709f0791c2d0dbe9b4430169ebb33ef73929d45273a7efe.
Candidate box: box@unresolved (BOX_REF=0.10.0).
Template registry: heavy-duty/rig-templates@4011092 (ref 4011092d8b23a2c8f077899325a98f3d84c275b9, snapshot) — the rig-templates source the converge read (#110/#153).
Instrument: drill/drill.sh, legs in execution order.

| Leg | Result |
| --- | --- |
| convergence — bootstrap staging-server reaches its role | PASS (35s) |
| re-converge (idempotence) | DIRTY — 2 state line(s) changed on the re-run |
| --host yes: pinned box installed, host stack up | PASS — heavy-duty/box@0.10.0, box doctor clean |
| test/db-integration.sh | SKIPPED — docker not installed — nothing to exercise |

Checks: 12 passed, 2 failed, 1 skipped.

Failed:
- FAIL: re-converge CHANGED the box — 2 state line(s) differ:
- FAIL: Docker package installation exited 0 but the daemon does not answer

Skipped — these did NOT run, and this record is not evidence for them:
- SKIP: db round-trip did not run — skip: docker not installed — nothing to exercise

The isolation boundary was NOT asserted here: it is box's drill's
assertion (heavy-duty/box drill/drill.sh), joined to this record by the run ID.

---

## Addendum — the operator's diagnosis of the same run

Everything above this rule is the instrument's own final stdout, committed
verbatim. Everything below it is @danmt's follow-up on the same run, quoted
from
[#211 (comment)](https://github.com/heavy-duty/rig/issues/211#issuecomment-5527549605)
and attributed rather than summarised. It is recorded here because
`drills/README.md` asks a record to state what failed, and a bare
`2 state line(s) differ:` does not say *which*:

> 1. The idempotence failure is a real first-run transition. After the first
>    bootstrap, `/etc/rig/role` contained `join-by=rig`; the immediate second
>    bootstrap rewrote it to `join-by=preexisting`. No other captured state
>    changed.
>
> 2. Docker installation succeeded, but the harness checked `docker info`
>    before the newly installed daemon was ready. The package operation
>    completed around 14:38:49 UTC and Docker reported ready at 14:38:50 UTC.
>    Docker and containerd remained active afterward. This appears to be a
>    readiness race in the drill scaffolding.
>
> The original stdout and bootstrap/Docker logs have been preserved locally
> with SHA-256 checksums. I have not rerun on the converged host because doing
> so would hide both first-run findings.

Neither failure is caused by the release diff, which is `VERSION` and
`CHANGELOG.md` and nothing executable. Both are properties of the tree that
`0.4.0` ships, and this record is the evidence for them rather than a claim
that they are absent. Note in particular that leg 4 is **SKIPPED, not PASS**:
`test/db-integration.sh` has no real-hardware evidence in this record, only
CI evidence at the same commit.
